# vessel-position-client

Vue 3 app that fetches vessel trips from `GET /positions/trips`.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

## Scripts

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run preview` — preview production build
